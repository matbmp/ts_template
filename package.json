{
  "name": "ts_template",
  "version": "1.0.0",
  "description": "Typescript command line project template",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "npm run build && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ts_template",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.3.1",
    "eslint": "^8.42.0",
    "typescript": "^5.1.3"
  }
}
